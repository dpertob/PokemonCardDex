import '../repository/collections_repository.dart';

class SyncCollections {
  //final CollectionsRepository repository;

  /*
  SyncCollections(this.repository);

  Future<void> call() async {
    final remoteCollections = await repository.fetchRemoteCollections();

    await repository.updateLocalCollections(remoteCollections);
  }
  */
}